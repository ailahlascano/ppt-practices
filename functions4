#include <iostream>
#include <conio.h>
using namespace std;

void duplicate (int& a, int& b, int&c)
{ 
	a*=2;
	b*=3;
	c*=4;
}

int main()
{
	int x = 1, y = 2, z = 3;
	duplicate (x, y, z);
	cout << "x = " << x << " , y = " << y << " , z = " << z;

	getch();
	return 0;
}
